# app

This directory contains the desktop application source for the rhythm-game assistant.

Use this area for code that ships inside the .exe application, not for browser pages.
Keep reusable logic separated into frontend, backend, models, and integration folders.
